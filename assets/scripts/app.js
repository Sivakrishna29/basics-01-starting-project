const defaultResult = 0;
let currenResult = defaultResult;
currenResult = currenResult + 10;

let calculationDescription = `(${defaultResult} + 10) * 3 / 2 - 1`;

outputResult(currenResult, calculationDescription);
